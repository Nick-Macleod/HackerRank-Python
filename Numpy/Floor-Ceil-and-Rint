# Enter your code here. Read input from STDIN. Print output to STDOUT
import numpy as np
np.set_printoptions(legacy='1.13')

# Create arrays with lists as arguments, so first make input into a list

A=np.array(list(map(float,input().split())))
#print(A)
print(np.floor(A))
print(np.ceil(A))
print(np.rint(A))
